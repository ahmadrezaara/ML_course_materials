\ProvidesPackage{uni-variable}[2023/02/23 ...]

\newif\if@mylang\@mylangfalse
\DeclareOption{en}{\@mylangtrue}
\DeclareOption*{\OptionNotUsed}
\ProcessOptions\relax

\if@mylang
    \newcommand{\initVars}{
        \gdef\@title{\sigline{title of document}}
        \gdef\@prof{\sigline{name of professor}}
        \gdef\@department{department: \sigline{department}}
        \gdef\@subject{\sigline{subject of document}}
        \gdef\@info{author: \sigline{author name}}
        \gdef\@subtitle{\,}
        \gdef\@mydate{date: \sigline{1400/00/00}}
        \gdef\@defsolvetitle{Soloution}
        \gdef\@Page{Page}
        \gdef\@of{of}
        \gdef\@End{End of}
    }
\else
    \newcommand{\initVars}{
        \gdef\@title{\sigline{file subject }}
        \gdef\@prof{\sigline{prof name }}
        \gdef\@department{department: \sigline{department name }}
        \gdef\@subject{\sigline{موضوع برگه عنوان}}
        \gdef\@info{author: \sigline{ author name}}
        \gdef\@subtitle{\,}
        \gdef\@mydate{date: \sigline{1400/00/00}}
        \gdef\@defsolvetitle{solution}
        \gdef\@Page{page}
        \gdef\@of{from}
        \gdef\@End{end}
    }
\fi

\initVars

\newcommand{\prof}[1]{\gdef\@prof{#1}}
\newcommand{\department}[1]{\gdef\@department{#1}}
\newcommand{\subject}[1]{\gdef\@subject{#1}}
\newcommand{\info}[1]{\gdef\@info{#1}}
\newcommand{\subtitle}[1]{\gdef\@subtitle{#1}}
\renewcommand{\date}[1]{\gdef\@mydate{#1}}

\gdef\@logo{logo/logo2}
\newcommand{\logo}[1]{\gdef\@logo{#1}}
\gdef\@logotwo{logo/logo2}
\newcommand{\logotwo}[1]{\gdef\@logotwo{#1}}